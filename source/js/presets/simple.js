const controls = ['play', 'volume', 'divider', 'timeline', 'divider', 'rate', 'download', 'fullscreen'];

export const preset = {
	options : {
		controls : {
			common : [
				controls,
				[]
			],
			fullscreen : [
				controls
			]
		}
	},
}
