const controls = ['play', 'volume', 'timeline', 'rate', 'download', 'fullscreen'];

export const preset = {
	options : {
		controls : {
			common : [
				controls,
				[]
			],
			fullscreen : [
				controls
			]
		}
	},
}
